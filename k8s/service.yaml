apiVersion: v1
kind: Service
metadata:
  name: javaproject-service
spec:
  type: LoadBalancer
  selector:
    app: javaproject
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8086